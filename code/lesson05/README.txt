README - 使い方説明

1. 必要なPython3のライブラリーをインストールします。
(すでにインストールされている場合、そのようなメッセージが表示され、何もインストールされません。)

python3 -m pip install pillow numpy opencv-python

2. 「画像」フォルダに加工したい画像ファイルを入れる。

3. ターミナルで
python3 image_processing.py
を実行する。
処理の種類ごとにフォルダが作成されて、その中に加工された画像ファイルが保存される。
