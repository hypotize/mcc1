{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "python3 -c \"\nimport json, sys, subprocess\nd = json.load(sys.stdin)\nf = d.get('tool_input', {}).get('file_path', '')\nif 'home.md' in f:\n    subprocess.run(['python3', 'scripts/update_history.py'])\n\""
          }
        ]
      }
    ]
  }
}
